specVersion: "v1beta"
serviceId: "rscps"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rscps-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
